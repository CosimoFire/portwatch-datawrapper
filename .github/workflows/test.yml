name: PortWatch → Datawrapper
on: [push]
jobs:
  run:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: pip install requests pandas
      - run: python portwatch_dw.py
        env:
          DW_TOKEN: ${{ secrets.DW_TOKEN }}
